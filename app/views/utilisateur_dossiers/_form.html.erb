<%= form_with(model: utilisateur_dossier) do |form| %>
  <% if utilisateur_dossier.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(utilisateur_dossier.errors.count, "error") %> prohibited this utilisateur_dossier from being saved:</h2>

      <ul>
        <% utilisateur_dossier.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <%= form.hidden_field :utilisateur_id, value: current_utilisateur.id %>

  <div>
    <%= form.label :nom, style: "display: block" %>
    <%= form.text_field :nom %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
